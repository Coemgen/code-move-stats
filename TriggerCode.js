function ReminderOrNewMonth(){
  // Determines if the process should only generate an email to remind staff to use the spreadsheet
  //  or run the code to create new spreadsheets
  monthlyRunMain(); // Creates new folders/files, sends email for Code Move and OHS Stats
}